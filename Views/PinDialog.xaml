<Window x:Class="RadcKioskLauncher.Views.PinDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Admin PIN" Height="220" Width="380" WindowStartupLocation="CenterScreen" ResizeMode="NoResize">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Admin PIN giriniz" FontSize="18" Margin="0,0,0,12"/>
        <PasswordBox x:Name="PinBox" Grid.Row="1" FontSize="22" MaxLength="12" />
        <TextBlock Grid.Row="2" Text=""/>
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="İptal" Width="96" Margin="0,0,8,0" Click="Cancel_Click"/>
            <Button Content="Tamam" Width="96" Click="Ok_Click"/>
        </StackPanel>
    </Grid>
</Window>
