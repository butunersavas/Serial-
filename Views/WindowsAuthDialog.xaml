<Window x:Class="RadcKioskLauncher.Views.WindowsAuthDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Windows Admin Kimlik Doğrulama" Height="280" Width="420" WindowStartupLocation="CenterScreen" ResizeMode="NoResize">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Yönetici kullanıcı adı" FontSize="15"/>
        <TextBox x:Name="UserBox" Grid.Row="1" FontSize="16" Margin="0,6,0,12"/>
        <TextBlock Text="Parola" Grid.Row="2" FontSize="15"/>
        <PasswordBox x:Name="PasswordBox" Grid.Row="3" FontSize="16" Margin="0,6,0,12"/>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="İptal" Width="96" Margin="0,0,8,0" Click="Cancel_Click"/>
            <Button Content="Doğrula" Width="96" Click="Ok_Click"/>
        </StackPanel>
    </Grid>
</Window>
