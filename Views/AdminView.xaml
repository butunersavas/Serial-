<UserControl x:Class="RadcKioskLauncher.Views.AdminView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Yönetici Paneli" FontSize="26" FontWeight="Bold" Margin="6"/>

        <DataGrid Grid.Row="1" Margin="0,10,0,10" AutoGenerateColumns="False" ItemsSource="{Binding Applications}"
                  SelectedItem="{Binding SelectedApp}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="*"/>
                <DataGridTextColumn Header="Başlık" Binding="{Binding Title}" Width="*"/>
                <DataGridTextColumn Header="Tür" Binding="{Binding Type}" Width="90"/>
                <DataGridTextColumn Header="Yol" Binding="{Binding Path}" Width="2*"/>
                <DataGridTextColumn Header="Argüman" Binding="{Binding Arguments}" Width="*"/>
                <DataGridTextColumn Header="Çalışma Dizini" Binding="{Binding WorkingDirectory}" Width="*"/>
                <DataGridCheckBoxColumn Header="Görünür" Binding="{Binding Visible}" Width="80"/>
                <DataGridCheckBoxColumn Header="Admin" Binding="{Binding RequiresAdmin}" Width="80"/>
                <DataGridTextColumn Header="Kategori" Binding="{Binding Category}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <GroupBox Grid.Row="2" Header="Whitelisted Windows Araçları" Foreground="White" Margin="0,0,0,10">
            <ItemsControl ItemsSource="{Binding SystemTools}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Button Content="{Binding Title}" Command="{Binding DataContext.LaunchToolCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}" CommandParameter="{Binding}"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </GroupBox>

        <StackPanel Grid.Row="3" Orientation="Vertical">
            <WrapPanel>
                <Button Content="Uygulama Ekle" Command="{Binding AddAppCommand}"/>
                <Button Content="Uygulama Sil" Command="{Binding RemoveAppCommand}"/>
                <Button Content="Yukarı" Command="{Binding MoveUpCommand}"/>
                <Button Content="Aşağı" Command="{Binding MoveDownCommand}"/>
                <Button Content="İkon Seç" Command="{Binding EditIconCommand}"/>
                <Button Content="Kategori Ekle" Command="{Binding AddCategoryCommand}"/>
                <Button Content="Config Doğrula" Command="{Binding ValidateConfigCommand}"/>
                <Button Content="Config Kaydet" Command="{Binding SaveCommand}"/>
                <Button Content="Kiosk Yenile" Command="{Binding RefreshScreenCommand}"/>
                <Button Content="Yeniden Yükle" Command="{Binding ReloadCommand}"/>
                <Button Content="Oturumu Kapat" Command="{Binding LogoutCommand}"/>
                <Button Content="Yeniden Başlat" Command="{Binding RebootCommand}"/>
                <Button Content="Kapat" Command="{Binding CloseAdminModeCommand}"/>
            </WrapPanel>
            <TextBlock Text="{Binding Status}" FontSize="14" Margin="8"/>
        </StackPanel>
    </Grid>
</UserControl>
